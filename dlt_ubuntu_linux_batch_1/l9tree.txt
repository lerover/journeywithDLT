=>tree (need to install )

sudo apt install tree

ls -l
tree

cd myzip/
ls -l
tree

tree -d    = List directories only (folder only)
tree -C    = colorize output (for core cmd)

ls --color =colorize output (for core cmd)

-------------------------------------------------